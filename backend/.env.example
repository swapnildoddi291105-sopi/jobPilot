# ─── Server ───────────────────────────────────────────────
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# ─── Supabase (REQUIRED) ──────────────────────────────────
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# ─── Gemini AI (REQUIRED for parsing + optimization + cover letters) ──
GEMINI_API_KEY=

# ─── Apify (REQUIRED for job scraping) ────────────────────
APIFY_API_TOKEN=
APIFY_ACTOR_ID=beOaHHMFnzMr6nGNU

# ─── Google Cloud (for Drive upload + Gmail send) ─────────
# Can be: raw JSON string, file path, or Render Secret File path
GOOGLE_APPLICATION_CREDENTIALS=
GOOGLE_DRIVE_FOLDER_ID=

# ─── n8n Admin (REQUIRED for n8n workflow automation) ─────
N8N_ADMIN_TOKEN=

# ─── JWT (for future auth tokens) ─────────────────────────
JWT_SECRET=
